var assert = require('stream-assert');
var should = require('should');
var zoneIdentifier = require('../');

require('mocha');

describe('gulp-zoneidentifier', function () {
  it('should equal 5', function () {
    (5).should.be.exactly(5);
  });
})
